{% extends 'WedBundle::base.html.twig' %}

{% block pageHeading %}Shoot Types{% endblock %}
{% block subheading %}Create a new shoot Type{% endblock %}

{% block content -%} <!-- Main content -->

    {{ form_start(form) }}
    <div class="col-md-6">

        {{ form_errors(form) }}
        <!-- general form elements -->
        <div class="box box-primary">
            <div class="box-header with-border">
                <h3 class="box-title">Create a new Type</h3>
            </div>
            <!-- /.box-header -->
            <!-- form start -->

                <div class="box-body">
                    {{ form_row(form.name) }}

                    {{ form_row(form._token) }}

                    <div class="box-footer">
                        {{ form_end(form) }}
                    </div>
                </div>
                <!-- /.box-body -->

        </div>
        <!-- /.box -->

    </div>

</div>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('types') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
